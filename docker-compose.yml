services:
  media-router:
    build: .
    container_name: media_router
    restart: unless-stopped
    ports:
      - "5080:5080"
    volumes:
      - .:/app
    environment:
      - MEDIA_ADMIN_PASSWORD=${MEDIA_ADMIN_PASSWORD:-}
